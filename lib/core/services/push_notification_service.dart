/// Compatibility export for the application push composition.
/// The application bootstrap still subscribes via `pushService.wakeSignals.listen`.
export 'package:delivery_app/app/bootstrap/push_notification_service.dart';
