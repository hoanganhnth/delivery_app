import 'dart:ui';

/// Legacy static color API retained until its external callers migrate.
class AppColors {
  static const primary = Color(0xFF007AFF);
  static const secondary = Color(0xFF34C759);
  static const error = Color(0xFFFF3B30);
}
